<project name="Revo-indeksoj" default="kapvortoj" basedir=".">
    <description>
       Reta Vortaro, reguloj por (re)krei indeksojn. 
    </description>

  <!-- set global properties for this build -->
  <property name="voko" location="/home/revo/voko"/>
  <property name="ant" location="${voko}/ant"/>
  <property name="revo" location="."/>
  <property name="inx" location="${revo}/inx"/>
  <property name="xml" location="${revo}/xml"/>
  <property name="voko_bin" location="${voko}/bin"/>
  <property name="xsl" location="${voko}/xsl"/>
  <property name="tmp" location="${revo}/.tmp"/>

  <target name="init">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${inx}" />
    <mkdir dir="${tmp}" />
    <!-- chu necesas refari la dosieron indekso.xml? -->
    <uptodate property="indekso.aktuala">
      <srcfiles dir="${xml}" includes="*.xml"/>
      <mapper type="merge" to="${tmp}/indekso.xml"/>
    </uptodate>
  </target>

  <target name="forigu" 
      description="forigas chiujn dosierojn en ${inx}">
 
     <delete>
        <fileset dir="${inx}" includes="*"/>
     </delete>
  </target>

  <target name="eltiro" unless="indekso.aktuala"
    description="faras la dosieron indekso.xml el chiuj artikoloj kiel bazo por chiuj indeksoj" 
    depends="init">

    <java classname="de.steloj.respiro.DirectoryTransformer"
     logError="true">
         <arg value="-v"/>
         <arg path="${xml}"/>
         <arg file="${xsl}/inx_eltiro.xsl"/>
         <arg file="${tmp}/indekso.xml"/>
         <classpath>
           <pathelement location="${ant}/respiro.jar"/>
           <pathelement path="${java.class.path}"/>
         </classpath>
    </java>

  </target>

  <target name="kategorioj" depends="init,eltiro"
     description="eltiri la kapvortojn, tradukojn, fakindikojn ktp. el la indeks-dosiero">
     <xslt in="${tmp}/indekso.xml" out="${tmp}/inx_kat.xml"
       style="${xsl}/inx_kategorioj.xsl"/>
  </target>

  <target name="ordigo" depends="init,kategorioj"
     description="ordigi la kapvortojn, tradukojn, fakojn ktp.">
     <xslt in="${tmp}/inx_kat.xml" out="${tmp}/inx_ord.xml"
       style="${xsl}/inx_ordigo.xsl"/>
  </target>

  <target name="kapvortoj" depends="init,ordigo"
     description="kreas la HTML-dosierojn por la alfabeta indekso">
     <xslt in="${tmp}/inx_ord.xml" out="${inx}/_eo.html"
       style="${xsl}/inx_kap.xsl"/>
  </target>

  <target name="cvs-shanghoj"
     description="kreas raporton pri la lastaj shanghoj per CVS">

     <cvschangelog dir="/home/revo/revo/xml"
                destfile="${tmp}/inx_shanghoj.xml"
                daysinpast="14"
     />
  </target>

</project>











